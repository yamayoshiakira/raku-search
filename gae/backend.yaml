# raku-search backend.yaml

env: standard

service: backend
runtime: python37

instance_class: B1
basic_scaling:
  max_instances: 1
  idle_timeout: 5m

handlers:
- url: /.*
  script: auto
  secure: always
